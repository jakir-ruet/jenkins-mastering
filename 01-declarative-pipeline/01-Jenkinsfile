pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
               echo "Pipeline Name: ${env.JOB_NAME}"
               echo 'Hello World'
            }
        }
    }
}